<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sesión - Sistema</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">
                        <h3>Panel de Usuario</h3>
                    </div>
                    <div class="card-body">
                        <% if (casa && casa.username) { %>
                            <div class="alert alert-success">
                                <h4>¡Bienvenido!</h4>
                                <p><strong>Usuario:</strong> <%= casa.username %></p>
                                <p><strong>Casa:</strong> <%= casa.nombre %></p>
                                <p><strong>Valor:</strong> $<%= casa.valor %></p>
                                <% if (casa.imagenURL) { %>
                                    <p><strong>Imagen:</strong> <a href="<%= casa.imagenURL %>" target="_blank">Ver imagen</a></p>
                                <% } %>
                            </div>
                            
                            <h5>Ejemplo de iteración con números:</h5>
                            <ul class="list-group mb-3">
                                <% const numeros = [1, 2, 3, 4, 5]; %>
                                <% numeros.forEach(function(numero) { %>
                                    <li class="list-group-item">Número: <%= numero %></li>
                                <% }); %>
                            </ul>
                            
                            <a href="/auth/logout" class="btn btn-danger">Cerrar Sesión</a>
                        <% } else { %>
                            <div class="alert alert-warning">
                                <h4>No hay sesión activa</h4>
                                <p>Debes iniciar sesión para acceder a esta página.</p>
                                <a href="/auth/login-vista" class="btn btn-primary">Ir al Login</a>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>